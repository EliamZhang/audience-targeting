## 新老客风险分高评级表（筛选排除）

以 ba.customer_profile_rawdata 为主表，构建「新老客风险分高评级表」。

处理顺序必须为：风险分表先去重 → 再关联主表 → 最后筛选风险分。

### 1. 主表处理

- 主表：ba.customer_profile_rawdata
- 筛选 application_time >= '2024-01-01'
- 排除 duedate_3m_30 = 1
- 主表所有字段均需保留。

### 2. 新客风险分先去重

- 表：model.tmp_eliam_risk_000006_vintage_monitor
- 使用字段：
  - application_id
  - aus_new_risk_bid_3rdmodel_v1_0_20251201
  - inserttime
- 按 application_id 分组，inserttime DESC 排序，仅保留最新一条。
- 去重后再通过 application_id 关联主表。

### 3. 老客风险分先去重

- 表：model.tmp_eliam_risk_000004_strategy_run_log
- 使用字段：
  - application_id
  - old_risk_bid_mltmodel_v1_1_20251021
  - inserttime
- 按 application_id 分组，inserttime DESC 排序，仅保留最新一条。
- 去重后再通过 application_id 关联主表。

### 4. 关联

- 将去重后的新客、老客风险分表分别通过 application_id 关联到主表。
- 不要先关联原始风险分表再去重，避免历史记录造成数据膨胀。

### 5. 最后筛选风险分

- 所有关联完成后，再统一筛选：
  - 新客分：0 <= aus_new_risk_bid_3rdmodel_v1_0_20251201 <= 指定阈值
  - 老客分：0 <= old_risk_bid_mltmodel_v1_1_20251021 <= 指定阈值
- 新客和老客条件按照实际业务逻辑进行组合筛选。

### 6. 最终保留字段

- 保留 ba.customer_profile_rawdata 主表的全部字段。
- 额外增加：
  - old_risk_bid_mltmodel_v1_1_20251021
  - aus_new_risk_bid_3rdmodel_v1_0_20251201

请严格按照 “风险分表先去重 → 再与主表关联 → 最后筛选风险分” 的顺序编写完整 SQL。

去重使用：

```sql
ROW_NUMBER() OVER (PARTITION BY application_id ORDER BY inserttime DESC)
```

---

## 新老客在贷表

以 ba.customer_profile_rawdata 为主表，构建「新老客在贷表」。

1. 筛选 status = 'Active_Account' 的在贷账户。
2. 通过 application_id 关联 ods.vintage_analysis_original_schedule_recoveries_v2。
3. 获取 scheduled_date，并按 application_id 取最大值，作为该笔申请的预计结清日。
4. 以当天日期为基准，筛选距离预计结清日不足 3 个月的 application_id。

---

## 4. 注册未完成申请

以 ba.customer_profile_rawdata 为主表，构建「新老客注册未完成申请 」。

5. 筛选出每个 user_id 最大的 application_id，然后筛选出

   ```sql
   COUNT(
       DISTINCT CASE
           WHEN application_status IN ('0.Incomplete', '1.In Progress')
           THEN application_id
       END
   ) AS incomplete_application
   ```

   然后就是未完成申请的。

---

## 5. 结清未复贷老户

以 ba.customer_profile_rawdata 为主表，构建「结清未复贷老户 」。

6. 筛选 出每个 status = 'Active_Account' 的在贷账户。
7. 命中你的价值模型的人
