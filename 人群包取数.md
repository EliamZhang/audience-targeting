# 人群包取数

## 通用约定

- 四个包**相互独立**，各自单独取数，包与包之间不需要互斥或去重。
- 输出粒度统一为**申请粒度**，一行一个 `application_id`。
- 主表字段已从全量精简为常用清单（见文末「附：主表输出字段清单」）；各包过程中算出的字段（包二的预计结清日、包三的 `incomplete_application`）一并输出。
- 取数结果文件命名统一为 `人群包<两位编号>_<口径描述>_<YYYY-MM-DD>.csv`，例如 `人群包01_新老客风险分高评级表_2026-09-24.csv`。编号补零，保证按文件名排序与包序一致。

---

## 一、新老客风险分高评级表（筛选排除）

**主表**：`ba.customer_profile_rawdata`

**新户 / 老户 判定**：用主表 `user_tag` 字段区分，取值 `'New'`（新户）/ `'Existing'`（老户）。新户去关联新客模型分，老户去关联老客模型分，两者互斥。

**处理顺序**：风险分表先去重 → 再关联主表 → 最后筛选风险分。顺序不能调换。

### 1. 主表处理

从主表 `ba.customer_profile_rawdata` 取数：

- 筛选条件：`application_time >= '2024-01-01'`
- 排除 `duedate_3m_30 = 1` 的记录（该字段是违约标记）。**只踢等于 1 的，其余取值（含 NULL）一律保留**：

  ```sql
  (duedate_3m_30 IS NULL OR duedate_3m_30 <> 1)
  ```

- 主表所有字段均需保留

### 2. 新客风险分先去重

- 来源表：`model.tmp_eliam_risk_000006_vintage_monitor`
- 适用对象：主表中 `user_tag = 'New'` 的申请
- 使用字段：
  - `application_id`
  - `aus_new_risk_bid_3rdmodel_v1_0_20251201`（新客风险分）
  - `inserttime`
- 去重方式：按 `application_id` 分组，按 `inserttime` 倒序，仅保留最新一条
- 去重完成后再通过 `application_id` 关联主表

### 3. 老客风险分先去重

- 来源表：`model.tmp_eliam_risk_000004_strategy_run_log`
- 适用对象：主表中 `user_tag = 'Existing'` 的申请
- 使用字段：
  - `application_id`
  - `old_risk_bid_mltmodel_v1_1_20251021`（老客风险分）
  - `inserttime`
- 去重方式：按 `application_id` 分组，按 `inserttime` 倒序，仅保留最新一条
- 去重完成后再通过 `application_id` 关联主表

### 4. 关联主表

- 按 `user_tag` 路由：`'New'` 的申请关联去重后的新客风险分表，`'Existing'` 的申请关联去重后的老客风险分表
- 关联键为 `application_id`
- 不要先关联原始风险分表再去重，否则历史记录会造成数据膨胀

### 5. 筛选风险分

两张风险分表都关联完成后，再统一加筛选条件。两个条件是**互斥路由**，不是 AND 也不是 OR：

- 新户（`user_tag = 'New'`）：按新客分筛选，`0 <= aus_new_risk_bid_3rdmodel_v1_0_20251201 <= 0.075629`
- 老户（`user_tag = 'Existing'`）：按老客分筛选，`0 <= old_risk_bid_mltmodel_v1_1_20251021 <= 0.05146`

即 `user_tag` 为 `'New'` 时只判新客分，为 `'Existing'` 时只判老客分，各自取并集。写成 SQL 就是：

```sql
(user_tag = 'New' AND aus_new_risk_bid_3rdmodel_v1_0_20251201 BETWEEN 0 AND 0.075629)
OR (user_tag = 'Existing' AND old_risk_bid_mltmodel_v1_1_20251021 BETWEEN 0 AND 0.05146)
```

### 6. 最终保留字段

- 按文末「附：主表输出字段清单」输出主表字段
- 额外增加两个字段：
  - `old_risk_bid_mltmodel_v1_1_20251021`
  - `aus_new_risk_bid_3rdmodel_v1_0_20251201`

### 编写要求

请严格按照「风险分表先去重 → 再与主表关联 → 最后筛选风险分」的顺序编写完整 SQL。去重统一使用：

```sql
ROW_NUMBER() OVER (PARTITION BY application_id ORDER BY inserttime DESC)
```

---

## 二、新老客在贷表

**主表**：`ba.customer_profile_rawdata`

1. 从主表筛选 `status = 'Active_Account'` 的在贷账户。
2. 在 `ods.vintage_analysis_original_schedule_recoveries_v2` 中先按 `application_id` 聚合，取 `scheduled_date` 的最大值，作为该笔申请的预计结清日。**必须先在子查询里聚合到 `application_id` 粒度，再关联主表**，否则一个申请对应多条还款计划会造成行数膨胀。
3. 以 `application_id` 为关联键，将上一步的聚合结果关联到主表。
4. 以 `CURRENT_DATE` 为基准日，筛选预计结清日**尚未到期**且**距今不足 3 个月**的 `application_id`：

   ```sql
   scheduled_date_max >= CURRENT_DATE
   AND scheduled_date_max < CURRENT_DATE + INTERVAL '3' MONTH
   ```

   已过期（预计结清日 < `CURRENT_DATE`）的不算在内。在贷账户理论上不应出现这种情况，属于存量异常数据。

---

## 三、新老客注册未完成申请

**主表**：`ba.customer_profile_rawdata`

口径：每个 `user_id` **只看最近一笔申请**，该笔未完成即入包，不统计该用户名下的其他申请。

1. 对每个 `user_id` 取最大的 `application_id`，即该用户最近的一笔申请（`application_id` 越大表示时间越靠后）。
2. 判断这一笔申请的 `application_status` 是否为未完成状态：

   ```sql
   application_status IN ('0.Incomplete', '1.In Progress')
   ```

3. 命中则该用户入包。输出标记字段：

   ```sql
   CASE
       WHEN application_status IN ('0.Incomplete', '1.In Progress') THEN 1
       ELSE 0
   END AS incomplete_application
   ```

---

## 四、新老客结清未复贷老户

**主表**：`ba.customer_profile_rawdata`

背景：一个 `user_id` 会对应多个 `application_id`，**`application_id` 越大表示时间越靠后**。

1. 本次筛选的用户，至少有一笔申请的 `status` 为 `Closed`。
2. 对每个 `user_id`，取 `status = 'Closed'` 的申请中 `application_id` 最大的那条，即该用户最后一次结清。
3. 检查这条之后（`application_id` 更大）是否还存在 `status = 'Active_Account'` 的申请。
4. 结清之后再没有出现在贷账户的，即为「结清未复贷老户」。

---

## 附：主表输出字段清单

从 `ba.customer_profile_rawdata` 全量字段中精简，只保留**进件相关**与**评级相关**字段，共 45 个。增删字段时，SQL 里四个包的 `base` CTE 需同步修改。

**标识**：`user_id`、`application_id`、`user_tag`、`application_tag`

**进件时间**：`application_time`、`application_date`

**进件状态**：`status`、`application_status`、`last_step`、`completed_step`、`completed_step_time`

**审批流转时间**：`risk_approved_time`、`risk_declined_time`、`converted_time`、`withdrawn_time`

**申请金额**：`requested_loan_amount`、`requested_loan_tag`

**模型分与风险等级**：`final_probability`、`risk_level`、`base_model_lightgbm_v2_prob`、`finv_predicted_probability_of_default`、`finv_risk_level`、`finv_color_code`、`finv_risk_tier`、`LTI`、`PTI`

**审批决策**：`approval_rules_decision`、`combined_ml_models_decision`、`is_auto_approved`、`combined_reasons`

**收入与负债**：`total_income`、`total_expenses`、`gross_surplus`、`net_surplus`

**客户属性**：`age`、`state`、`suburb`、`zip_code`、`dependents`

**违约与结清**：`is_bad_loan`、`duedate_3m_30`、`scheduled_cleared_date`、`final_closed_date`

**渠道**：`traffic_source`、`app_platform`

### 精简掉的部分

- `doc_req_*` / `admin_req_*`：文档与 KYC 请求明细，8 组共 50 多个字段
- 放款与还款计划：`dispersal_date`、`payout_date`、`payout_amount`、`total_loan_amount`、`principal`、`repayment_amount`、`repayments`、`repayment_frequency`、`first_payment_date`、`last_payment_date`
- `estimate_principal_remaining_mob0` ~ `mob4` 与 `dpd_days_*_mob0` ~ `mob4`：15 个账龄跟踪字段
- `base_model_gen2_lightgbm_v1_prob`、`base_model_gen3_lightgbm_v1_prob`、`base_probability`
- `dpd_7_probability` ~ `dpd_49_probability`：7 个逾期概率
- `finv_approval_*`、`finv_predicted_*`、`finv_is_*` 等审批与预测明细
- `attributed_*`、`cost_category`：营销归因明细
- `default_income` / `default_expenses` / `default_gross_surplus` 等默认值字段
- `application_week` / `application_month`、`dispersal_week` / `dispersal_month` 等派生时间字段
- `last_status` / `last_status_time` / `assessment_status` / `approval_tag` / `status_transition`
- `reapplication_activation_date`、`probation_period`

需要哪个说一声，加回去就行。
