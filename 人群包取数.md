# 人群包取数

## 一、新老客风险分高评级表（筛选排除）

**主表**：`ba.customer_profile_rawdata`

**处理顺序**：风险分表先去重 → 再关联主表 → 最后筛选风险分。顺序不能调换。

### 1. 主表处理

从主表 `ba.customer_profile_rawdata` 取数：

- 筛选条件：`application_time >= '2024-01-01'`
- 排除 `duedate_3m_30 = 1` 的记录
- 主表所有字段均需保留

### 2. 新客风险分先去重

- 来源表：`model.tmp_eliam_risk_000006_vintage_monitor`
- 使用字段：
  - `application_id`
  - `aus_new_risk_bid_3rdmodel_v1_0_20251201`（新客风险分）
  - `inserttime`
- 去重方式：按 `application_id` 分组，按 `inserttime` 倒序，仅保留最新一条
- 去重完成后再通过 `application_id` 关联主表

### 3. 老客风险分先去重

- 来源表：`model.tmp_eliam_risk_000004_strategy_run_log`
- 使用字段：
  - `application_id`
  - `old_risk_bid_mltmodel_v1_1_20251021`（老客风险分）
  - `inserttime`
- 去重方式：按 `application_id` 分组，按 `inserttime` 倒序，仅保留最新一条
- 去重完成后再通过 `application_id` 关联主表

### 4. 关联主表

- 以 `application_id` 为关联键，将去重后的新客风险分表、老客风险分表分别关联到主表
- 不要先关联原始风险分表再去重，否则历史记录会造成数据膨胀

### 5. 筛选风险分

两张风险分表都关联完成后，再统一加筛选条件：

- 新客分：`0 <= aus_new_risk_bid_3rdmodel_v1_0_20251201 <= 指定阈值`
- 老客分：`0 <= old_risk_bid_mltmodel_v1_1_20251021 <= 指定阈值`
- 新客条件与老客条件之间如何组合（AND / OR），按实际业务逻辑确定

### 6. 最终保留字段

- `ba.customer_profile_rawdata` 主表的全部字段
- 额外增加两个字段：
  - `old_risk_bid_mltmodel_v1_1_20251021`
  - `aus_new_risk_bid_3rdmodel_v1_0_20251201`

### 编写要求

请严格按照「风险分表先去重 → 再与主表关联 → 最后筛选风险分」的顺序编写完整 SQL。去重统一使用：

```sql
ROW_NUMBER() OVER (PARTITION BY application_id ORDER BY inserttime DESC)
```

---

## 二、新老客在贷表

**主表**：`ba.customer_profile_rawdata`

1. 从主表筛选 `status = 'Active_Account'` 的在贷账户。
2. 以 `application_id` 为关联键，关联 `ods.vintage_analysis_original_schedule_recoveries_v2`。
3. 取每个 `application_id` 下 `scheduled_date` 的最大值，作为该笔申请的预计结清日。
4. 以当天日期为基准，筛选预计结清日距今不足 3 个月的 `application_id`。

---

## 三、新老客注册未完成申请

**主表**：`ba.customer_profile_rawdata`

1. 对每个 `user_id` 取其最大的 `application_id`，即该用户最近的一笔申请。
2. 在该范围内统计未完成申请数：

```sql
COUNT(
    DISTINCT CASE
        WHEN application_status IN ('0.Incomplete', '1.In Progress')
        THEN application_id
    END
) AS incomplete_application
```

3. `incomplete_application` 即该用户的「注册未完成申请」数量。

---

## 四、新老客结清未复贷老户

**主表**：`ba.customer_profile_rawdata`

背景：一个 `user_id` 会对应多个 `application_id`。

1. 本次筛选的用户，至少有一笔申请的 `status` 为 `Closed`。
2. 对每个 `user_id`，定位该用户最后一次出现 `Closed` 的那笔申请。
3. 检查这笔 `Closed` 之后，是否还存在 `status = 'Active_Account'` 的申请。
4. 结清之后再没有出现在贷账户的，即为「结清未复贷老户」。
